
{% block title %}Stage{% endblock %}

{% block body %}
    <h1>Stage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stage.id }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ stage.dateDebut ? stage.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Date_fin</th>
                <td>{{ stage.dateFin ? stage.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Type_du_stage</th>
                <td>{{ stage.typeDuStage }}</td>
            </tr>
            <tr>
                <th>Nom_encadrant</th>
                <td>{{ stage.nomEncadrant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stage_index') }}">back to list</a>

    <a href="{{ path('stage_edit', {'id': stage.id}) }}">edit</a>

    {{ include('stage/_delete_form.html.twig') }}
{% endblock %}
